"""Stage H: the one-off confirmatory run on the sealed CONF split (pre-flight P3, with P5).

Design and every rule the code follows: ``docs/stage_h_design.md``. Entry point:
``python -m nhs_ae.evaluate.stage_h COMMAND``. This package imports nothing at top level, so
one module can be imported (and tested) without the others.
"""
